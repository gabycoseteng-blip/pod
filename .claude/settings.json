{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pip install -q -r \"${CLAUDE_PROJECT_DIR:-.}/tools/requirements.txt\" >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}

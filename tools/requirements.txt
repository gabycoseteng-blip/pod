# For tools/upload_audio.py — Cloudflare R2 uploads via the S3-compatible API.
boto3>=1.34
# Portable ffmpeg fallback so routine/render_gemini.py can encode MP3 even when
# the environment has no system ffmpeg (it auto-detects this package).
imageio-ffmpeg>=0.4
